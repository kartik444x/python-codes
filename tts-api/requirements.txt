# ─────────────────────────────────────────────────────────────
# Production TTS API — Python Dependencies
# Generated from pyproject.toml for direct pip install.
# Last reviewed: 2026-09
#
# Install:
#   pip install -r requirements.txt
#
# For development extras:
#   pip install -r requirements.txt -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────

# ── Web Framework ────────────────────────────────────────────
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9

# ── Data Validation ──────────────────────────────────────────
pydantic>=2.7.0,<3.0
pydantic-settings>=2.3.0,<3.0

# ── ML / TTS ─────────────────────────────────────────────────
# PyTorch: install the right variant for your hardware.
# CPU-only (smaller, works everywhere):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# The line below installs the default (CPU or CUDA based on your system):
torch>=2.2.0

# Kokoro TTS (hexgrad/Kokoro-82M — Apache 2.0)
kokoro>=0.9.4
# Grapheme-to-phoneme for English (required by kokoro)
misaki[en]>=0.9.4

# ── Audio Processing ─────────────────────────────────────────
soundfile>=0.12.1        # WAV read/write — requires libsndfile (usually bundled)
numpy>=1.26.0,<3.0
pydub>=0.25.1            # MP3 conversion — requires ffmpeg system binary
scipy>=1.13.0            # Resampling, normalization

# ── Database ──────────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0
alembic>=1.13.0
aiosqlite>=0.20.0        # Async SQLite (development)
asyncpg>=0.29.0          # Async PostgreSQL (production)

# ── Caching / Queue ───────────────────────────────────────────
redis[hiredis]>=5.0.0
rq>=1.16.0

# ── HTTP Client ───────────────────────────────────────────────
httpx>=0.27.0

# ── Security ─────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# ── Observability ─────────────────────────────────────────────
prometheus-client>=0.20.0
structlog>=24.1.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
anyio>=4.4.0
aiofiles>=23.2.0
tenacity>=8.3.0
ulid-py>=1.1.0

# ── Rate Limiting ─────────────────────────────────────────────
slowapi>=0.1.9
limits>=3.12.0
